# Spec: Apple Reminders Web Clone

## 1. Overview

Apple Reminders 앱의 핵심 기능을 웹으로 구현한 프로젝트.
사용자가 브라우저에서 리마인더를 생성, 관리, 정리할 수 있는 풀스택 애플리케이션.

| 구분 | 기술 스택 |
|------|----------|
| Backend | Spring Boot 4.0.3, Java 25, JPA, H2 |
| Frontend | Next.js (Latest), React, TypeScript, Tailwind CSS |
| 통신 | REST API (JSON) |

---

## 2. 핵심 기능

### 2.1 리스트 (List) 관리

- 리스트 CRUD (생성 / 조회 / 수정 / 삭제)
- 리스트별 색상 및 아이콘 지정
- 기본 제공 스마트 리스트:
  - **오늘 (Today)** — 오늘 마감인 리마인더
  - **예정 (Scheduled)** — 마감일이 설정된 리마인더
  - **전체 (All)** — 모든 리마인더
  - **완료됨 (Completed)** — 완료 처리된 리마인더
  - **깃발 표시 (Flagged)** — 깃발 표시된 리마인더

### 2.2 리마인더 (Reminder) 관리

- 리마인더 CRUD
- 속성:
  - 제목 (title) — 필수
  - 메모 (notes) — 선택
  - 마감일 (due date) — 선택
  - 마감 시간 (due time) — 선택
  - 우선순위 (priority) — 없음 / 낮음 / 보통 / 높음
  - 깃발 표시 (flagged) — boolean
  - 완료 여부 (completed) — boolean
- 리스트 내 리마인더 순서 지정 (drag & drop 정렬)

### 2.3 검색

- 제목 / 메모 기반 리마인더 검색

---

## 3. 데이터 모델

### ReminderList

| 필드 | 타입 | 설명 |
|------|------|------|
| id | Long (PK) | 자동 생성 |
| name | String | 리스트 이름 |
| color | String | HEX 색상 코드 (e.g. `#007AFF`) |
| icon | String | 아이콘 이름 |
| displayOrder | Integer | 사이드바 정렬 순서 |
| createdAt | LocalDateTime | 생성일시 |
| updatedAt | LocalDateTime | 수정일시 |

### Reminder

| 필드 | 타입 | 설명 |
|------|------|------|
| id | Long (PK) | 자동 생성 |
| title | String | 제목 (필수) |
| notes | String (TEXT) | 메모 |
| dueDate | LocalDate | 마감일 |
| dueTime | LocalTime | 마감 시간 |
| priority | Enum | NONE, LOW, MEDIUM, HIGH |
| flagged | Boolean | 깃발 표시 |
| completed | Boolean | 완료 여부 |
| completedAt | LocalDateTime | 완료 시각 |
| displayOrder | Integer | 리스트 내 정렬 순서 |
| listId | Long (FK) | 소속 리스트 |
| createdAt | LocalDateTime | 생성일시 |
| updatedAt | LocalDateTime | 수정일시 |

---

## 4. API 설계

### 리스트 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/lists` | 전체 리스트 조회 |
| POST | `/api/lists` | 리스트 생성 |
| PUT | `/api/lists/{id}` | 리스트 수정 |
| DELETE | `/api/lists/{id}` | 리스트 삭제 (소속 리마인더 포함) |
| PATCH | `/api/lists/reorder` | 리스트 순서 변경 |

### 리마인더 API

| Method | Endpoint | 설명 |
|--------|----------|------|
| GET | `/api/lists/{listId}/reminders` | 리스트별 리마인더 조회 |
| GET | `/api/reminders/today` | 오늘 마감 리마인더 |
| GET | `/api/reminders/scheduled` | 예정된 리마인더 |
| GET | `/api/reminders/all` | 전체 리마인더 |
| GET | `/api/reminders/completed` | 완료된 리마인더 |
| GET | `/api/reminders/flagged` | 깃발 표시 리마인더 |
| GET | `/api/reminders/search?q={query}` | 검색 |
| POST | `/api/lists/{listId}/reminders` | 리마인더 생성 |
| PUT | `/api/reminders/{id}` | 리마인더 수정 |
| PATCH | `/api/reminders/{id}/complete` | 완료 토글 |
| PATCH | `/api/reminders/{id}/flag` | 깃발 토글 |
| DELETE | `/api/reminders/{id}` | 리마인더 삭제 |
| PATCH | `/api/reminders/reorder` | 순서 변경 |

---

## 5. UI/UX 상세 (Apple Reminders 준수)

> 목표: macOS/iOS Apple Reminders 앱과 최대한 동일한 룩앤필을 웹에서 재현한다.

### 5.1 전체 레이아웃

```
┌──────────────────────────────────────────────────────────────┐
│  Sidebar (280px)      │  Content Area                        │
│                       │                                      │
│  🔍 검색 바            │  ┌─ Reminder List ──┬─ Detail ─────┐ │
│                       │  │                  │              │ │
│  ┌ 스마트 리스트 ────┐ │  │  ○ 장보기         │  제목         │ │
│  │ 📅 오늘     (3)  │ │  │    우유, 계란     │  메모         │ │
│  │ 📋 예정          │ │  │  ○ 회의 준비      │  마감일       │ │
│  │ 📁 전체     (12) │ │  │  ● 이메일 보내기  │  우선순위     │ │
│  │ ✅ 완료됨        │ │  │    ↳ 완료됨       │  깃발         │ │
│  │ 🚩 깃발 표시     │ │  │                  │              │ │
│  └──────────────────┘ │  │  + 리마인더 추가   │              │ │
│                       │  └──────────────────┴──────────────┘ │
│  ┌ 내 리스트 ────────┐ │                                      │
│  │ 🔴 개인          │ │                                      │
│  │ 🔵 업무          │ │                                      │
│  │ 🟢 쇼핑          │ │                                      │
│  └──────────────────┘ │                                      │
│                       │                                      │
│  + 리스트 추가        │                                      │
└──────────────────────────────────────────────────────────────┘
```

- **사이드바** — 고정 너비 280px, 왼쪽 고정, 스크롤 독립
- **컨텐츠 영역** — 리마인더 목록 + 우측 디테일 패널 (선택 시 표시)
- 디테일 패널이 닫힌 상태에서는 리마인더 목록이 전체 너비 차지

### 5.2 사이드바 디자인

#### 스마트 리스트 카드 그리드

사이드바 상단에 스마트 리스트를 **2x2 + 1 카드 그리드**로 배치 (Apple Reminders와 동일):

```
┌──────────┬──────────┐
│ 📅 오늘   │ 📋 예정   │
│    3     │    5     │
├──────────┼──────────┤
│ 📁 전체   │ 🚩 깃발   │
│   12     │    2     │
├──────────┴──────────┤
│ ✅ 완료됨            │
│    8                │
└─────────────────────┘
```

- 각 카드: 둥근 모서리 (12px), 왼쪽 상단에 원형 아이콘, 우측 상단에 카운트 (큰 볼드 숫자)
- 카드 하단에 라벨 텍스트
- 카드 색상:
  - 오늘 = `#007AFF` (파란색)
  - 예정 = `#FF3B30` (빨간색)
  - 전체 = `#5856D6` (보라색, 다크모드에서 어울리게)
  - 깃발 표시 = `#FF9500` (주황색)
  - 완료됨 = `#8E8E93` (회색)
- hover 시 약간 밝아지는 효과 (`opacity` 또는 `brightness`)

#### 내 리스트 섹션

- "내 리스트" 섹션 헤더 (작은 회색 대문자)
- 각 리스트 행: 좌측에 **채워진 원형 아이콘** (리스트 색상), 리스트 이름, 우측에 리마인더 카운트
- 선택된 리스트는 배경색 하이라이트 (`bg-gray-200` / 다크모드 `bg-gray-700`)
- 리스트 우클릭(컨텍스트 메뉴): 이름 변경, 색상 변경, 삭제

#### 하단 "리스트 추가" 버튼

- 사이드바 하단 고정
- `+ 리스트 추가` 텍스트 + 아이콘, Apple 스타일 텍스트 버튼

### 5.3 리마인더 목록 뷰

#### 리스트 헤더

- 리스트 이름을 **큰 굵은 텍스트** (24-28px, bold)로 표시
- 리스트 색상과 동일한 텍스트 컬러
- 우측 상단에 `...` 더보기 메뉴 (정렬 옵션, 리스트 정보)

#### 리마인더 행

각 리마인더는 다음 구조:

```
┌─────────────────────────────────────────────┐
│  ○  장보기                           🚩     │
│     2024-03-29 오후 3:00 · 우선순위 높음      │
│     우유, 계란 사기                           │
└─────────────────────────────────────────────┘
```

- **체크 원** — 빈 원형 (`○`), 리스트 색상의 테두리. hover 시 살짝 채워짐
  - 완료 시: 리스트 색상으로 채워진 원 + 체크마크 (`✓`), 제목에 취소선, 텍스트 회색 처리
  - 완료 애니메이션: 원이 채워지면서 체크마크 나타남 → 0.5초 후 페이드아웃하며 목록에서 사라짐
- **제목** — 첫 번째 줄, 일반 굵기
- **부가 정보** — 두 번째 줄 (작은 회색 텍스트): 마감일시, 우선순위 (`!` / `!!` / `!!!`)
- **메모 미리보기** — 세 번째 줄 (작은 회색 텍스트, 1줄 말줄임)
- **깃발 아이콘** — 우측에 주황색 깃발 (flagged인 경우만)
- 행 사이 얇은 구분선 (`border-b`, 좌측 체크 원 이후부터 시작 — Apple 스타일 들여쓰기 구분선)

#### 인라인 리마인더 추가

- 목록 하단에 `+ 새로운 리마인더` 텍스트 (리스트 색상)
- 클릭 시 해당 자리에 빈 입력 행이 나타남 (체크 원 + 텍스트 input)
- Enter로 저장 후 다음 새 입력 행이 이어서 나타남
- Escape로 취소

### 5.4 디테일 패널

리마인더 클릭 시 우측에 슬라이드-인으로 나타나는 편집 패널:

```
┌─────────────────────────────┐
│  제목 입력 필드              │
│  메모 입력 필드 (textarea)   │
│─────────────────────────────│
│  📅 날짜    2024-03-29      │
│  ⏰ 시간    오후 3:00       │
│  🚩 깃발    ○ (토글)        │
│  ‼️ 우선순위  높음 ▾         │
│─────────────────────────────│
│  📁 리스트   업무 ▾          │
│─────────────────────────────│
│  🗑️ 리마인더 삭제            │
└─────────────────────────────┘
```

- 각 항목은 Apple Settings 스타일의 **그룹화된 행** (둥근 모서리 카드 내 구분선으로 분리)
- 제목/메모는 상단에 자유 입력
- 날짜/시간은 토글로 켜고 끈 후 값 입력
- 우선순위는 세그먼트 컨트롤 또는 드롭다운 (`없음 | ! | !! | !!!`)
- 리스트 이동은 드롭다운 선택
- 하단에 빨간색 "리마인더 삭제" 버튼
- 변경사항은 **자동 저장** (debounce 500ms)

### 5.5 리스트 생성/편집 모달

리스트 추가 또는 편집 시 중앙 모달:

```
┌───────────────────────────┐
│     🔴 (큰 원형 미리보기)   │
│                           │
│  [리스트 이름 입력]         │
│                           │
│  색상 선택:                │
│  🔴🟠🟡🟢🔵🟣🩷🩶🟤⚫🩵🩷  │
│                           │
│  아이콘 선택:              │
│  📋 📌 🛒 💼 🏠 ❤️ ...     │
│                           │
│  [취소]          [완료]    │
└───────────────────────────┘
```

- 12가지 프리셋 색상 (Apple Reminders와 동일한 팔레트)
- 아이콘은 카테고리별 그리드로 선택
- 상단에 선택한 색상+아이콘의 큰 원형 미리보기

### 5.6 검색

- 사이드바 상단 검색 바 (돋보기 아이콘 + 텍스트 입력)
- 입력 시 실시간 필터링 (debounce 300ms)
- 검색 결과는 컨텐츠 영역에 리스트별로 그룹핑하여 표시
- 매칭 텍스트 하이라이트

### 5.7 컨텍스트 메뉴 & 스와이프 액션

- 리마인더 행 우클릭: 완료, 깃발 토글, 삭제, 리스트 이동
- (모바일 대응 시) 좌 스와이프 → 삭제(빨간), 우 스와이프 → 완료(초록)/깃발(주황)

### 5.8 빈 상태 (Empty State)

- 리스트에 리마인더가 없을 때: 중앙에 큰 아이콘 + "리마인더 없음" 텍스트 (연한 회색)
- 검색 결과 없을 때: "결과 없음" 표시

### 5.9 애니메이션 & 트랜지션

| 상황 | 애니메이션 |
|------|-----------|
| 리마인더 완료 | 원 채움 + 체크마크 → 0.5초 후 페이드아웃+슬라이드업 |
| 리마인더 삭제 | 좌로 슬라이드아웃 + 페이드 |
| 디테일 패널 열기/닫기 | 우측에서 슬라이드-인/아웃 (200ms ease) |
| 리스트 전환 | 컨텐츠 영역 크로스페이드 (150ms) |
| 드래그 앤 드롭 | 드래그 중 그림자 + 살짝 기울어짐, 드롭 시 스냅 |
| 모달 열기/닫기 | 페이드 + 스케일 (0.95 → 1.0) |
| 사이드바 카드 hover | 밝기 증가 (brightness 1.05) |

### 5.10 색상 팔레트 (프리셋)

Apple Reminders와 동일한 12색:

| 이름 | Light | Dark |
|------|-------|------|
| Red | `#FF3B30` | `#FF453A` |
| Orange | `#FF9500` | `#FF9F0A` |
| Yellow | `#FFCC00` | `#FFD60A` |
| Green | `#34C759` | `#30D158` |
| Cyan | `#5AC8FA` | `#64D2FF` |
| Blue | `#007AFF` | `#0A84FF` |
| Indigo | `#5856D6` | `#5E5CE6` |
| Purple | `#AF52DE` | `#BF5AF2` |
| Pink | `#FF2D55` | `#FF375F` |
| Brown | `#A2845E` | `#AC8E68` |
| Gray | `#8E8E93` | `#98989D` |
| Black | `#1C1C1E` | `#F2F2F7` |

### 5.11 타이포그래피 & 간격

- 폰트: `SF Pro` 폴백 → `-apple-system, BlinkMacSystemFont, 'Segoe UI', system-ui, sans-serif`
- 리스트 제목: 28px, bold
- 리마인더 제목: 15px, regular
- 부가 정보 / 메모 미리보기: 13px, `text-gray-500`
- 스마트 리스트 카운트: 24px, bold
- 행 높이: 최소 44px (터치 타겟)
- 사이드바 리스트 행: 36px
- 여백: 16px 기본 패딩, 섹션 간 24px

### 5.12 다크모드

- `prefers-color-scheme: dark` 미디어 쿼리로 자동 전환
- 배경: `#000000` (사이드바), `#1C1C1E` (컨텐츠), `#2C2C2E` (디테일/카드)
- 텍스트: `#FFFFFF` (주), `#98989D` (보조)
- 구분선: `#38383A`
- 카드/모달: `#2C2C2E` 배경, `#38383A` 테두리
- 프리셋 색상은 Dark 컬럼 값 사용

---

## 6. 프로젝트 구조

```
reminder/
├── src/main/java/jaemin/ai/jaeminreminder/   # Spring Boot Backend
│   ├── controller/
│   ├── service/
│   ├── repository/
│   ├── entity/
│   └── dto/
├── src/main/resources/
│   └── application.properties
├── frontend/                                  # Next.js Frontend
│   ├── src/
│   │   ├── app/                               # App Router
│   │   ├── components/
│   │   ├── lib/                               # API client, utils
│   │   └── types/
│   ├── package.json
│   └── tailwind.config.ts
└── build.gradle.kts
```

---

## 7. 개발 순서

| Phase | 내용 |
|-------|------|
| **Phase 1** | Backend — Entity, Repository, Service, Controller, API 완성 |
| **Phase 2** | Frontend — Next.js 프로젝트 셋업, 레이아웃, 사이드바, 리스트 뷰 |
| **Phase 3** | Frontend — 리마인더 CRUD, 상세 패널, 완료/깃발 토글 |
| **Phase 4** | Frontend — 스마트 리스트, 검색, 드래그 앤 드롭 |
| **Phase 5** | 다크모드, 애니메이션, 반응형 대응, 마무리 |

---

## 8. 제약사항 및 결정

- **인증 없음** — 싱글 유저 로컬 앱으로 운영 (H2 인메모리 DB)
- **알림 없음** — 브라우저 알림/푸시는 범위 밖
- **반복 리마인더 없음** — v1 범위에서 제외
- **서브태스크 없음** — v1 범위에서 제외
- Backend 포트: `8080`, Frontend 포트: `3000`
- Frontend에서 Backend API 호출 시 Next.js rewrites로 프록시 처리
